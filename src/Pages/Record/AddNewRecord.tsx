import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {Text, View} from 'react-native';

export const AddNewRecord = ({navigation}: NativeStackScreenProps<any>) => {
  return (
    <View>
      <Text>Test</Text>
    </View>
  );
};
